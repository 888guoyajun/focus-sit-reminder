# 接线说明 v0.1

## 信号定义

- `BTN_PIN`：用户确认/响应按钮
- `LED_PIN`：提醒指示灯
- `BUZZER_PIN`：蜂鸣器
- `MOTOR_PIN`：振动马达或舵机控制

## 建议接法

- 按钮一端接 GPIO，另一端接 GND，使用内部上拉
- LED 串 220Ω 电阻后接 GPIO
- 蜂鸣器接 GPIO 和 GND
- 振动马达建议通过三极管驱动，不要直接硬接 GPIO

## 结构原则

- 所有执行器默认低功耗
- 先做低强度提醒，再升级
- 第一版只验证行为闭环

